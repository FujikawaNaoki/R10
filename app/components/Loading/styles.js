import { StyleSheet } from 'react-native'
import { dimensions } from '../../config/styles'

export default StyleSheet.create({
  activityIndicator: {
    marginTop: dimensions.screenHeight/2 - 100,
  }
})